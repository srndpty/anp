# AGENTS.md

anp の開発規約。人間と AI エージェントの両方がこれに従う。

## プロジェクト概要

**anp**（annotator for pdf）は、技術書・数学書・問題集を読むための個人用デスクトップ PDF リーダー。

> 学習向けの PDF リーダー。非破壊アノテーションと強力なダークモードを備える。

- PDF ファイル自体は既定で変更しない。学習メタデータは SQLite に別途保存する
- 主対象は Windows 11 デスクトップ
- Adobe Acrobat の置き換えを目指すものではない

### 非目標

次の機能は作らない。要望があっても、まずこの一覧を確認すること。

PDF のテキスト編集 / 画像編集 / ページの挿入・削除 / 結合・分割 / OCR / 電子署名 /
フォーム編集 / クラウド同期 / 共同編集 / 手書き / Acrobat 互換のアノテーション書き出し / AI 機能

## 言語規約

**人間が読むことを前提とした文章はすべて日本語で書く。**

| 対象 | 言語 |
|---|---|
| AI チャットの返答 | 日本語 |
| コードコメント・docstring | 日本語 |
| コミットメッセージ・PR・Issue | 日本語 |
| ドキュメント | 日本語 |
| UI の表示文字列 | 日本語 |
| 識別子（変数・関数・クラス・モジュール名） | 英語 |
| ログメッセージ・例外メッセージ | 英語 |

例外メッセージのうち、利用者にダイアログで見せるものは日本語にする。

## 技術スタック

Python 3.12+ / PySide6（QtPdf, QtWidgets）/ SQLite / uv

C++・Rust・Electron・Web フロントエンド・別の PDF エンジンは、具体的な技術的理由がない限り導入しない。

## 開発コマンド

```bash
uv sync --all-groups          # 依存関係の導入
uv run anp                    # アプリの起動
uv run pytest                 # テスト
uv run pytest --cov=anp       # カバレッジ付き
uv run ruff check .           # lint
uv run ruff format .          # 整形
uv run mypy                   # 型チェック
uv run pre-commit install     # フックの導入（最初に一度）
```

## アーキテクチャ

```
anp/
├── app.py        アプリケーション境界（起動・ログ初期化・未捕捉例外）
├── core/         パス・ログ・設定
├── pdf/          PDF ドメイン（レイアウト・レンダリング・キャッシュ）
├── storage/      SQLite への永続化
└── ui/           ウィジェットとイベント処理
```

**依存の向きは `ui` → `pdf` / `storage` / `core` の一方向。** 逆流させない。
`pdf` と `storage` は `ui` を知らない。

| 層 | 責務 | 持ってはいけないもの |
|---|---|---|
| `core` | パス解決、ログ設定、設定の読み書き | ドメイン知識 |
| `pdf` | ページ座標の計算、レンダリング、キャッシュ | ウィジェット、SQL |
| `storage` | SQLite の接続とスキーマ | Qt ウィジェット、PDF 知識 |
| `ui` | 描画・入力イベント・ダイアログ | SQL、PDF の内部構造 |

座標は **PDF ページ相対または正規化座標**で保持する。ビューポートのピクセル座標を
永続化してはならない（ズームやリサイズで壊れるため）。

## 設計方針

### DRY と責務の分離

- 大きな god class を作らない。1つのクラスには1つの責務
- UI のイベント処理を、永続化や PDF ドメインのロジックと混ぜない

### オーバーエンジニアリングを避ける

**これは上の項目と同じくらい重要。**

- 使う予定のない抽象化・インターフェース・基底クラス・プラグイン機構を先回りして作らない
- 実装が2箇所に増えてから共通化する。1箇所しかないうちは共通化しない
- 「将来こう拡張できるかも」だけを理由にコードを書かない。将来の要件は、責務の
  分離によって**塞がない**ようにするだけでよい

### フォールバックと互換性は最小限

**開発中のため、後方互換の維持は不要。**

- 古いバージョンを吸収するコード、レガシーデータの移行、非推奨 API の温存は書かない
- データ形式が変わったら、移行コードを書くのではなく作り直してよい
- ライブラリの機能が存在するかを実行時に調べる分岐は書かない。使えるバージョンを
  pyproject.toml で要求する

### エラー処理

- 広い `except Exception` はアプリケーション境界（`app.py`）でのみ許す。必ずログを伴う
- ライブラリが戻り値でエラーを返すなら、例外に変換せず戻り値で分岐する
  （例: `QPdfDocument.load()` は `Error` 列挙を返す）
- 利用者に見せるエラーは日本語で、何が起きたかを具体的に書く

### 状態

- 隠れたグローバル可変状態を作らない。依存はコンストラクタで受け取る
- テストしたいものは、Qt ウィジェットに触らない純粋なクラス/関数として切り出す

### Qt の作法

- Qt のオブジェクト所有権とスレッド親和性に従う
- **ワーカースレッドから GUI オブジェクトを操作しない**
- `paintEvent` の中で重い処理（画素変換・ブロッキング待ち・ファイル I/O）を行わない。
  高コストな結果はキャッシュし、描画は転送のみにする
- **キャンセルできない非同期 API には、要求を積む前に上限を設ける**（範囲の限定・
  重複の抑止・デバウンス）。「後で結果を捨てる」は対策にならない
- 開いた PDF/ドキュメントのリソースは確実に解放する

### 型

- すべての関数に型ヒントを付ける（`disallow_untyped_defs` で強制）
- `from __future__ import annotations` を使う

## テスト方針

- 純粋なロジックは `QApplication` なしで決定的にテストする
- GUI のタイミングに依存するテスト（`qtbot.wait` の連発など）は避ける。
  シグナルの発火や状態を直接検証する
- GUI テストは `QT_QPA_PLATFORM=offscreen` で動かす
- テスト関数名は英語、docstring で日本語の意図を書く

## コミット規約

Conventional Commits の型を使い、**本文は日本語**で書く。

```
feat: 連続スクロールのページビューを追加

可視ページ±1ページだけをレンダリング要求するようにした。
QPdfPageRenderer にキャンセルAPIがないため、要求を積む前に抑制している。
```

型: `feat` / `fix` / `refactor` / `perf` / `test` / `docs` / `chore` / `ci`

## Windows 前提

- パスは `pathlib.Path` を使う（ruff の `PTH` で強制）
- 125% / 150% の DPI スケーリングを考慮する。レンダリング要求は
  論理サイズ × `devicePixelRatio()` で行い、`QImage.setDevicePixelRatio()` を設定する
- ログとデータベースは `%LOCALAPPDATA%\anp` に置く（ローミングさせない）
